
**[오류 수정 내용]**

1. `insertArrayList` - 루프가 `al->size`부터 시작하여 `al->data[size+1]` 범위 초과 → `al->size - 1`로 수정
2. `initArrayList` - `int` 반환형인데 `return` 없음 → `return 1` 추가
3. `addSparseMatrix` - 루프 조건 `<=` 사용으로 마지막 인덱스 범위 초과 → `<`로 수정
4. `addSparseMatrix` - sm2에만 있는 원소를 결과에 추가하는 코드 누락 → 로직 추가
5. `addElementSparseMatrix`, `printSparseMatrix` - `int` 반환형인데 `return` 없음 → `return 1` 추가
